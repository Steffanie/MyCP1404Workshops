def triangleWorkouterer(base, length):
    base = float(input("Enter base length: "))
    height = int(input("Enter height length: "))
    area = (base * height) / 2
    return area

print(triangleWorkouterer(0,0))